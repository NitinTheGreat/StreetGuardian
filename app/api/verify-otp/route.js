// import { NextResponse } from 'next/server'

// export async function POST(request) {
//   const { email, otp } = await request.json()

//   // Here you would typically verify the OTP against what's stored in your database
//   // For this example, we'll just pretend it's always correct

//   // If OTP is correct, finalize user registration
//   // This is where you would typically save the user to your database

//   return NextResponse.json({ message: 'OTP verified successfully' }, { status: 200 })
// }